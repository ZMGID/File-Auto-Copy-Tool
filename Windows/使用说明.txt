# Windows版 - 文件自动复制工具使用说明

## 使用方法
双击运行 `文件自动复制工具_Windows版.bat`

## 程序功能
- 自动识别文件名末尾的x2、x4等标记
- 自动复制相应数量的文件（x2=复制1份，x4=复制3份）
- **支持所有文件类型**（STL、TXT、JPG、PDF等）
- 避免重复复制已存在的文件
- 支持拖拽文件夹操作

## 使用步骤
1. 双击运行 `文件自动复制工具_Windows版.bat`
2. 选择包含文件的文件夹：
   - **直接回车**: 使用程序所在目录（如果有STL文件夹会自动选择）
   - **输入路径**: 手动输入完整文件夹路径
   - **拖拽文件夹**: 直接拖拽文件夹到命令窗口
3. 程序自动处理所有需要复制的文件
4. 完成！

## 示例
- `part_x2_.stl` → `part_x2_.stl` + `part_x2__copy1.stl`（总共2份）
- `document_x3_.pdf` → 原文件 + 2个复制文件（总共3份）
- `image_x4_.jpg` → 原文件 + 3个复制文件（总共4份）

## 系统要求
- Windows 7/8/10/11
- 无需额外安装任何软件

## 打包EXE版本
如果需要单个exe文件：
1. 双击运行 `自动打包EXE.bat`
2. 等待自动安装和打包完成
3. 在 `dist` 文件夹中找到 `文件自动复制工具.exe`

## 注意事项
- 程序会自动创建带有"_copy1"、"_copy2"等后缀的复制文件
- 可以多次运行，不会重复复制已存在的文件
- 支持任何文件类型，只要文件名符合 `_x数字_` 格式
- 完美支持中文路径和文件名
- 支持数字x2到x20的复制标记